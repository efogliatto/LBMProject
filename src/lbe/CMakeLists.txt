# Basic LBE library
add_subdirectory (basic)

# Liang model
add_subdirectory (liangModel)

# Li model (pseudopotential)
add_subdirectory (liModel)

# Test model (no force. vdW sound speed)
add_subdirectory (testModel)